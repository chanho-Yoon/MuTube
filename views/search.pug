extends layouts/main
include mixins/videoBlock

block content
  .search__header
    h3 검색 : #{search_word}
  .search__videos
    each video in videos
      +videoBlock({
        title:video.title,
        views:video.views,
        videoFile:video.videoFile
      })
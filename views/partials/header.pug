header.header
  .header__column
    a(href=routes.home)
      i.fas.fa-play-circle

  .header__column
    form(action=routes.search, method="get")
      input(type="text", placeholder="Search.." name="search_word")
  .header__column
    ul
      if !user.idCheck
        li
          a(href=routes.join) 회원가입
        li
          a(href=routes.login) 로그인
      else
        li
          a(href=routes.upload) 업로드
        li
          a(href=routes.userDetail(user.id)) 프로필
        li
          a(href=routes.logout) 로그아웃